# 从看得懂，到能独立写出来

这条路线面向“会一点 if / else，但不能独立写程序”的学习者。先建立小步反馈，再练习把陌生问题拆开。题库覆盖 Python 常用语法、数据分析库、Web 基础和算法进阶；Python 生态及 ACM 竞赛没有靠固定题数就能全部学完的终点。

## 每一题的学习闭环

1. **读懂**：先读语法小课，用自己的话复述输入、输出和边界。遇到陌生语法，先在当前题里写最短的实验，运行自定义输入。
2. **手算**：拿公开示例，写出每一步变量的值。循环至少走三轮；递归画出两层调用；动态规划列出一个很小的表。
3. **动手**：先写正确的直观做法，再分析会不会超时。卡住时每次只展开一条提示，先尝试再看下一条。
4. **检查**：运行示例后，自己补空输入、一个元素、重复值、负数、极值等适用边界，再提交全部测试。自定义输入没有标准答案，通过执行不代表结果正确。
5. **复盘**：在笔记写下“为什么错、哪条规则漏了、怎样构造反例”。看过答案后，隔开一段时间，清空代码再独立重写。

平台会指出实际错误和结果差异，但不能自动理解所有算法意图。请把“测试通过”与“我能解释正确性、复杂度并处理新输入”分开检查。

手算后可点击 Python 编辑器上的“逐步观察”，对照每行执行前的变量值，用“上一步／下一步”检查自己的推演。建议先猜下一步的值，再点击验证。记录较长时缩小自定义输入；观察运行只帮助理解，不代替独立提交。

## 六个阶段与验收标准

| 阶段 | 学习范围 | 不看答案时应能做到 |
|---|---|---|
| 1. 编程起步 | 第 1–9 章，1–90 题 | 独立写函数；处理分支、循环、列表、字典；会读 traceback；能拆解简单文字需求 |
| 2. Python 工具与数据 | 第 10–18 章，91–180 题 | 选择合适容器；查懂库函数参数；解释数组形状、缺失值处理、训练集与测试集的区别 |
| 3. 复试机考基础 | 第 19–24 章，181–240 题 | 解析多组输入；实现二分、BFS、DFS、基础 DP；说明为什么结束、为什么不会漏解 |
| 4. Web 与工程入门 | 第 25–30 章，241–300 题 | 写 SQL；知道 HTTP 状态码、JSON、前后端职责；处理 API 失败；能解释一次浏览器请求的完整路径 |
| 5. 秋招专题 | 第 31–36 章，301–360 题 | 从题意识别窗口、二分答案、堆、DP 等模式；能举出贪心反例；能分析时空复杂度并完成限时练习 |
| 6. ACM 进阶 | 第 37–42 章，361–420 题 | 自己写完整输入输出；使用进阶结构和算法；给出不变量或转移依据；赛后独立补题 |

第 2 阶段中的科学计算和第 4 阶段的 Web 可以按目标穿插；它们不是进入算法训练的强制门槛。零基础不要直接跳进高级算法。遇到某章看不懂，先回到其前置章节补缺口，而不是背完整答案。

## 从函数模式切换到 ACM 模式

函数题：平台已经准备好参数，你写 `def solve(...)`，通过 `return` 返回结果。调试时可以 `print()`。

ACM 题：你自己解析标准输入，输出程序的答案。先核对题目是单组、先给 T 组、直到 EOF，还是遇到终止标记结束。`print()` 中的额外提示语也会被当作答案，调试改写到 `sys.stderr`。

```python
import sys

def main():
    # 本例输入为两个整数。实际题目要严格按照其格式解析。
    a, b = map(int, sys.stdin.buffer.read().split())
    print(a + b)

if __name__ == "__main__":
    main()
```

不要因为一个小样例运行很快，就认定算法复杂度符合要求。先根据题面规模估计操作次数；对 n 较大的题，尤其注意双重循环、列表头部删除、字符串反复拼接和递归深度。

## 三个独立项目验收

这些是题库之外的开放任务，用来检验迁移能力，没有自动判题。先自己完成，再请老师、同学或代码审查工具检查设计和实现。

**阶段 1 后：命令行学习记录器。** 支持添加、查询、修改学习记录，保存 JSON；重启后能读回；文件缺失和格式损坏时给出可理解的提示。验收重点：函数拆分、输入校验、异常处理、文件读写。

**阶段 2 后：小型数据分析报告。** 找一份非敏感 CSV，说明列含义，清理缺失值，分组统计，绘制两张有坐标含义的图。如果训练模型，先划分训练集与测试集，说明评价指标和数据泄漏风险。验收重点：数据处理有依据，结论可以从数据复现。

**阶段 4 后：全栈任务清单。** 使用 Python HTTP 服务、SQLite 和浏览器页面实现增删改查；前端通过 fetch 调用 API；错误响应有清晰状态码；数据库查询使用参数绑定。验收重点：刷新后数据保留、空列表与网络失败都有界面提示、不把用户输入拼接进 SQL。

## 进入限时训练后

先浏览全部题目，估计难度并安排时间；不必按顺序解答。某题停滞时保存草稿，先处理更有把握的题。限时训练重在暴露薄弱点，训练后的补题与重写同样重要。

每周挑选几道做错、看过答案或者解释不清的题，隔天重做。能够通过修改输入构造反例、解释复杂度、独立写出相同思路，才算这一类问题逐渐掌握。进一步冲击真实比赛时，还需要更多陌生题、完整比赛和系统性证明训练；本题库提供这段路线的基础与进阶练习，不承诺比赛名次或秋招结果。
